# `TopLoading`

`TopLoading` is a simple React functional component that renders a fixed top-loading bar at the top of the page. This component provides a visual cue for loading activity by displaying a thin bar at the top of the screen. It can be used to indicate when content is loading or a process is ongoing.

## Usage

```tsx
<TopLoading />
```